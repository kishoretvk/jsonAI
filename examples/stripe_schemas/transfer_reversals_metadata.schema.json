{
  "title": "transfer_reversals_metadata",
  "type": "object",
  "properties": {
    "transfer_reversal_id": {
      "type": "string",
      "description": "The ID of the transfer_reversal that this metadata entry is associated with."
    },
    "key": {
      "type": "string",
      "description": "Name of the metadata key."
    },
    "value": {
      "type": "string",
      "description": "Value of the metadata."
    }
  },
  "required": ["transfer_reversal_id", "key", "value"]
}
